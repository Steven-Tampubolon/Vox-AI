version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - errcheck
    - gocritic
  exclusions:
    rules:
      - path: cli/banner.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
